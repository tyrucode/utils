//I am not sure if the assignment was asking us to just create our own random string manipulation function but that is what I am doing
export const caps = function (str) {
    const capstr = str.toUpperCase();
    console.log(capstr);
};